[package]
name = "braidpool-common"
version = "0.1.0"
edition = "2021"
description = "Common utilities shared across Braidpool workspace crates"
license = "MIT OR Apache-2.0"

[dependencies]
bitcoin = { workspace = true }

[features]
default = []
